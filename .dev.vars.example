# Copiar a `.dev.vars` y completar con el connection string real del Postgres.
# `.dev.vars` está gitignoreado: nunca commitear valores.
CLOUDFLARE_HYPERDRIVE_LOCAL_CONNECTION_STRING_HYPERDRIVE=postgresql://USER:PASSWORD@HOST:5432/DB?sslmode=require
