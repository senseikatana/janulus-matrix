# Opcional y apagado por defecto. Sin esta key la app usa la cadena 100% gratuita.
# La key vive SOLO en el server (runtimeConfig privada). Nunca usar NUXT_PUBLIC_ para secrets.
NUXT_TRANSLATE_API_KEY=
